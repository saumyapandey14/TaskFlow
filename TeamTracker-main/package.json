{
  "name": "ethara-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager by Ethara AI - Full-stack React + Express app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "client:dev": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.2",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
